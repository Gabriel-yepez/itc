{
  "kind": "collectionType",
  "collectionName": "competencies",
  "info": {
    "singularName": "competency",
    "pluralName": "competencies",
    "displayName": "Competency",
    "description": "Competencias principales destacadas en tarjetas"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text",
      "required": true
    },
    "icon": {
      "type": "string",
      "description": "Clave del icono a renderizar en el frontend (ej. code, shield, chart)"
    },
    "accent": {
      "type": "enumeration",
      "enum": ["primary", "secondary", "tertiary", "neutral"],
      "default": "primary"
    },
    "order": {
      "type": "integer",
      "default": 0
    }
  }
}
